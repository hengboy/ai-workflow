interface:
  display_name: "Planning"
  short_description: "Freeze a reviewed spec and plan for a new or ambiguous feature."
  default_prompt: "Use $planning to clarify this new or ambiguous request and freeze its spec and plan."
